/* 
firebase project steps:
//=========================================
steps:

1. create a project (with meaning full name).
2. fulfill the requirement.
3. go to does right corner of the page > build > web > get started > install the firebase js SDK
> install sdk > npm install firebase > register to the firebase > and continue > come to project setting > go to general setting > copy the firebase initialization > create a file in src > then copy the firebase initialization > export default app > import {getAuth} from "firebase/auth"(to app.js) > const auth = getAuth = getAuth(app) (in App.js) > console.firebase > authentication(below setting) > setStated > go to google > enable this name and save

=============================
SDK : software development kit 
=============================
 */